import React from 'react'

const Buttons = ({title}) => {
  return (
    <div>
        <button className='p-1 px-3 m-2 bg-gray-200 rounded-lg'>{title}</button>
    </div>
  )
}

export default Buttons